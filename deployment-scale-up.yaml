apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: my-app-v2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app-v2
  template:
    metadata:
      labels:
        app: my-app-v2
    spec:
      containers:
        - name: my-container
          image: nginx:latest
          ports:
            - containerPort: 80